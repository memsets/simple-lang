# simple-lang

```
value = 100

function add(a, b) {
  return a + b
}

function predicate(a) {
  if (a > value) {
    return true
  }
  return false
}

print(add(5, 5)) // "10"
print(predicate(105)) // "true"

```

